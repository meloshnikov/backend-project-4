const downloadPage = (url, option) => {
  console.log('🚀 : run downloadPage : downloadPage:');
  console.log('🚀 : downloadPage : url:', url);
  console.log('🚀 : downloadPage : option:', option);
};

export default downloadPage;
